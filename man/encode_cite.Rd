% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cite.R
\name{encode_cite}
\alias{encode_cite}
\title{Create ENCODE citation metadata}
\usage{
encode_cite(
  x,
  format = c("table", "text", "markdown", "bibtex"),
  style = c("summary", "methods", "supplement"),
  enrich = "auto",
  max_enrich_datasets = 10,
  quiet = FALSE
)
}
\arguments{
\item{x}{ENCODE accession(s), file metadata, download result, search result,
or ENCODE object.}

\item{format}{Output format: `"table"`, `"text"`, `"markdown"`, or
`"bibtex"`.}

\item{style}{Presentation style for text and markdown outputs.}

\item{enrich}{Whether file-table citations should fetch parent experiment
metadata to fill lab, institution, project, organism, and biosample fields
when possible. Use `"auto"` to enrich small file tables, `TRUE` to always
enrich, or `FALSE` to avoid extra web requests.}

\item{max_enrich_datasets}{Maximum number of parent experiment datasets to
enrich automatically when `enrich = "auto"`.}

\item{quiet}{If `FALSE`, print concise messages.}
}
\value{
A data frame for `format = "table"`; otherwise a character vector
  containing text, markdown, or BibTeX-like entries.
}
\description{
Build a table or short text summary describing the ENCODE datasets and files
used in an analysis. The output is intended for methods sections,
supplements, and reproducibility records.
}
\details{
ENCODE accessions are data records, not publications, so this function reports
dataset/file attribution and links rather than inventing article-style
citations.
}
\examples{
files <- data.frame(
  experiment_accession = "ENCSR000AAA",
  file_accession = "ENCFF000AAA",
  lab = "Example lab",
  assay_title = "total RNA-seq",
  file_format = "txt",
  output_type = "metadata",
  status = "released"
)
encode_cite(files, enrich = FALSE)
encode_cite(files, format = "text")

# Use enrich = "auto" for small real file tables when experiment context is useful:
# files <- encode_list_files("ENCSR284QGB", file_format = "tsv")
# encode_cite(files, enrich = "auto")
}
