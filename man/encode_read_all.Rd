% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read.R
\name{encode_read_all}
\alias{encode_read_all}
\title{Read all files in a completed download table}
\usage{
encode_read_all(
  files,
  max_size = "100MB",
  region = NULL,
  allow_large = FALSE,
  unsupported = c("return_path", "error"),
  as = c("auto", "data.frame", "GRanges", "path"),
  quiet = FALSE,
  ...
)
}
\arguments{
\item{files}{A file table with a \code{local_path} column.}

\item{max_size}{Maximum uncompressed size to read into memory, as bytes or a
string. Use \code{NULL} for no cap. Compressed text is scanned before import.}

\item{region}{Optional genomic range passed to \code{rtracklayer::import()} as
\code{which} for indexed genomic formats.}

\item{allow_large}{Whether to import a complete BigWig or BigBed without a
region.}

\item{unsupported}{Use \code{"return_path"} to return an \code{encode_local_file}
object for unsupported input, or \code{"error"} to fail.}

\item{as}{Return form for BED-like input: \code{"auto"}, \code{"data.frame"},
\code{"GRanges"}, or \code{"path"}.}

\item{quiet}{If \code{FALSE}, print concise progress messages.}

\item{...}{Additional arguments passed to tabular or rtracklayer readers.}
}
\value{
An \code{encode_loaded_files} list with \code{metadata} and \code{data} components.
}
\description{
Read each \code{local_path} with \code{encode_read()} and retain the file metadata.
No tables are simplified or combined automatically.
}
\examples{
path <- tempfile(fileext = ".tsv")
writeLines(c("gene_id\tvalue", "Gata4\t10"), path)
files <- data.frame(
    file_accession = "ENCFFLOCAL1",
    file_format = "tsv",
    local_path = path
)
encode_read_all(files, quiet = TRUE)
}
