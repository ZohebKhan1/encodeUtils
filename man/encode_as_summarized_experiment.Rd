% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read.R
\name{encode_as_summarized_experiment}
\alias{encode_as_summarized_experiment}
\title{Convert prepared quantification tables to a SummarizedExperiment}
\usage{
encode_as_summarized_experiment(
  x,
  feature_id = "gene_id",
  assays = "raw_counts"
)
}
\arguments{
\item{x}{An \code{encode_loaded_files} object containing data frames.}

\item{feature_id}{Complete, unique feature identifier column shared by every
table.}

\item{assays}{Numeric column names to combine into assays.}
}
\value{
A \code{SummarizedExperiment} with file metadata in \code{colData()} and query
provenance in \code{metadata()}.
}
\description{
Convert prepared quantification tables to a SummarizedExperiment
}
\examples{
path <- tempfile(fileext = ".tsv")
writeLines(c("gene_id\texpected_count", "Gata4\t10"), path)
files <- data.frame(
    file_accession = "ENCFFLOCAL1",
    file_format = "tsv",
    local_path = path
)
loaded <- encode_prepare_quant(encode_read_all(files, quiet = TRUE))
encode_as_summarized_experiment(loaded)
}
