% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search.R
\name{encode_count}
\alias{encode_count}
\title{Count ENCODE search matches}
\usage{
encode_count(
  type = "Experiment",
  filters = list(),
  search = NULL,
  status = "released",
  metadata = c("basic", "full"),
  quiet = FALSE
)
}
\arguments{
\item{type}{ENCODE object type to search, such as `"Experiment"` or `"File"`.
Use `"Experiment"` to find datasets and `"File"` to find individual files.
Use `NULL` only for mixed free-text searches.}

\item{filters}{Named list of ENCODE search filters. Raw ENCODE filter names
are supported, including dot notation and negation such as
`"control_type!="`.}

\item{search}{Optional free-text search term.}

\item{status}{Optional ENCODE status filter. The default keeps released
records only. Use `NULL` to omit the status filter.}

\item{metadata}{How much linked metadata to request. The default `"basic"`
keeps the count request small because no result rows are returned.}

\item{quiet}{If `FALSE`, print a concise query status message.}
}
\value{
A query count. Printing shows the total number of matching records.
}
\description{
Return only the number of records that match a search. This is useful as a
quick preflight for broad queries before asking for many rows or before using
`limit = "all"`.
}
\details{
Most interactive workflows can start with `encode_search()`, which already
reports both the number returned and the total number available.
}
\examples{
# This mocked response keeps the example offline and runnable.
count_json <- paste0(
  '{"@graph":[],"total":12,',
  '"filters":[{"field":"type","term":"Experiment"}]}'
)
count <- httr2::with_mocked_responses(
  function(req) httr2::response(
    200,
    headers = "Content-Type: application/json",
    body = charToRaw(count_json)
  ),
  encode_count(quiet = TRUE)
)
count$total_results

# Use count as an optional safety check for broad searches:
# encode_count(type = "File", filters = list(file_format = "fastq"))
}
