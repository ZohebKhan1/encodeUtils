% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{encode_report}
\alias{encode_report}
\title{Build an ENCODE metadata table}
\usage{
encode_report(
  fields,
  type = "Experiment",
  filters = list(),
  search = NULL,
  status = "released",
  limit = 25,
  metadata = c("full", "basic"),
  endpoint = c("search", "report"),
  allow_large = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{fields}{Character vector of ENCODE field names to include.}

\item{type}{ENCODE object type.}

\item{filters}{Named list of ENCODE query filters.}

\item{search}{Optional free-text search term.}

\item{status}{Optional ENCODE status filter. Use `NULL` to omit.}

\item{limit}{Number of search rows to request, or `"all"`.}

\item{metadata}{Amount of linked metadata to request when
`endpoint = "search"`. `"full"` returns more readable linked fields.
`"basic"` requests a smaller response.}

\item{endpoint}{Either `"search"` or `"report"`.}

\item{allow_large}{Must be `TRUE` to use `endpoint = "report"`.}

\item{quiet}{If `FALSE`, print a concise status message.}
}
\value{
A metadata report. `encode_results()` extracts the report table.
}
\description{
Return selected ENCODE metadata fields as a table.
}
\details{
The default `endpoint = "search"` is bounded by `limit`. The optional
`endpoint = "report"` uses ENCODE's report TSV endpoint and requires
`allow_large = TRUE` because the portal may return many rows.
}
\examples{
# Offline example.
search_json <- paste0(
  '{"@graph":[{"accession":"ENCSR000AAA",',
  '"assay_title":"total RNA-seq","status":"released"}],"total":1}'
)
report <- httr2::with_mocked_responses(
  function(req) httr2::response(
    200,
    headers = "Content-Type: application/json",
    body = charToRaw(search_json)
  ),
  encode_report(
    fields = c("accession", "assay_title"),
    limit = 1,
    quiet = TRUE
  )
)
encode_results(report)

# Live ENCODE example:
# encode_report(
#   fields = c("accession", "assay_title", "biosample_summary"),
#   type = "Experiment",
#   search = "mouse heart ChIP-seq",
#   limit = 10
# )
}
