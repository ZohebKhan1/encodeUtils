% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{encode_preview_download}
\alias{encode_preview_download}
\title{Preview an ENCODE download}
\usage{
encode_preview_download(
  x,
  file_accession = NULL,
  n = NULL,
  directory = NULL,
  cache = TRUE,
  max_file_size = "2GB",
  max_total_size = "5GB",
  allow_unknown_size = FALSE,
  prefer_cloud = FALSE,
  n_largest = 5,
  quiet = FALSE
)
}
\arguments{
\item{x}{ENCFF accession(s), file metadata table, file search result,
selected-file object, or experiment object.}

\item{file_accession}{Optional ENCODE file accession(s), such as
`"ENCFF260OJQ"`, to include from `x`. Use this when you want specific files
rather than the first `n` rows.}

\item{n}{Optional number of files to include from the top of `x`.}

\item{directory}{Destination directory. If `NULL`, a package cache directory
from `tools::R_user_dir("encodeUtils", "cache")` is used.}

\item{cache}{Whether `directory = NULL` should use the package cache. If
`FALSE`, a session temporary directory is used.}

\item{max_file_size}{Maximum allowed size per file, as bytes or a string like
`"500MB"`.}

\item{max_total_size}{Maximum allowed total size, as bytes or a string.}

\item{allow_unknown_size}{Whether the eventual download plan may include files
with missing `file_size` metadata without listing an override requirement.}

\item{prefer_cloud}{Whether to prefer ENCODE cloud URLs when available.}

\item{n_largest}{Number of largest known-size files to include in the plan.}

\item{quiet}{If `FALSE`, print a concise status message.}
}
\value{
A download plan.
}
\description{
Preview files before download. New code can usually use
`encode_download(..., dry_run = TRUE)` instead; this helper remains for code
that wants the older plan summary with largest files and required overrides.
}
\examples{
download_marker <- paste0(intToUtf8(64), intToUtf8(64), "download")
files <- data.frame(
  file_accession = "ENCFF000AAA",
  href = paste0(
    "/files/ENCFF000AAA/",
    download_marker,
    "/ENCFF000AAA.txt"
  ),
  file_size = 3,
  md5sum = "900150983cd24fb0d6963f7d28e17f72"
)
encode_preview_download(files, directory = tempdir())
encode_preview_download(files, n = 1, directory = tempdir())

# Live ENCODE example:
# selected <- encode_select_files(files, preset = "raw_fastq")
# encode_preview_download(selected, directory = "data/encode")
}
