# Scripts

This directory contains optional scripts for reproducing package examples and
test fixtures.

- `get_started_workflow.R` shows small ENCODE RNA-seq, ATAC-seq, and ChIP-seq
  examples.
- `refresh_test_fixtures.R` refreshes offline test fixtures from ENCODE when
  `ENCODEUTILS_REFRESH_FIXTURES=true`.
