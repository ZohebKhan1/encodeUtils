# Example data

These two small synthetic gene-quantification tables support offline examples,
tests, and the introductory vignette. They are not ENCODE records and must not
be used for biological analysis.

`files.csv` supplies the corresponding minimal ENCODE-like metadata. The
download URLs are placeholders used only to demonstrate dry-run planning.
